export const selectCategory = (state) => state.filters.categories;
export const selectSearchCategory = (state) => state.filters.selectedCategory;
export const selectGender = (state) => state.filters.gender;
export const selectSearchQuery = (state) => state.filters.searchQuery;
export const selectSpecies = (state) => state.filters.species;
